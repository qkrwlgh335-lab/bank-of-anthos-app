apiVersion: apps/v1
kind: Deployment
metadata:
  name: userservice
spec:
  template:
    spec:
      containers:
        - name: userservice
          envFrom:
            - secretRef:
                name: bank-app-secrets
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: contacts
spec:
  template:
    spec:
      containers:
        - name: contacts
          envFrom:
            - secretRef:
                name: bank-app-secrets
